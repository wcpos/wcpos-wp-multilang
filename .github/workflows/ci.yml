name: CI

on:
  push:
    branches:
      - main
      - 'codex/**'
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'languages/**'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'languages/**'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare local dependency stubs
        run: |
          mkdir -p ../woocommerce-pos ../woocommerce-pos-pro

          # The WCPOS free plugin is what this integration hooks into, so CI runs against the
          # real wp.org release rather than a stub. A header-only stub defines neither
          # wcpos_request() nor Sync\Store_Scope, so every v2-lane test skipped and the gate
          # proved nothing — CI was green having exercised only the v1 route branch.
          if [ ! -f ../woocommerce-pos/woocommerce-pos.php ]; then
            curl -sSfL -o "${RUNNER_TEMP}/woocommerce-pos.zip" https://downloads.wordpress.org/plugin/woocommerce-pos.zip
            unzip -q "${RUNNER_TEMP}/woocommerce-pos.zip" -d ..
          fi

          if [ ! -f ../woocommerce-pos-pro/woocommerce-pos-pro.php ]; then
            printf '%s\n' '<?php' '/**' ' * Plugin Name: WCPOS Pro (Stub)' ' */' > ../woocommerce-pos-pro/woocommerce-pos-pro.php
          fi

      - name: Run tests
        run: pnpm test

      - name: Run lint
        run: pnpm wp-env run tests-cli --env-cwd='wp-content/plugins/wcpos-wp-multilang' -- composer run lint

      - name: Stop wp-env
        if: always()
        run: pnpm wp-env stop
